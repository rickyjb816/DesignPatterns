<div class="top-row ps-3 navbar navbar-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="">DesignPatterns</a>
        <button title="Navigation menu" class="navbar-toggler" @onclick="ToggleNavMenu">
            <span class="navbar-toggler-icon"></span>
        </button>
    </div>
</div>

<div class="@NavMenuCssClass" @onclick="ToggleNavMenu">
    <nav class="flex-column">
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="" Match="NavLinkMatch.All">
                <span class="oi oi-home" aria-hidden="true"></span> Home
            </NavLink>
        </div>
        <div style="color: white;">
            Creation
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="singleton">
                Singleton
            </NavLink>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="factory">
                Factory
            </NavLink>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="builder">
                Builder
            </NavLink>
        </div>
        <div style="color: white;">
            Structural
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="adaptor">
                Adaptor
            </NavLink>
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="decorator">
                Decorator
            </NavLink>
        </div> 


        <div style="color: white;">
            Behavioural
        </div>
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="chainofresponibility">
                Chain Of Responibility
            </NavLink>
        </div>
    </nav>
</div>

@code {
    private bool collapseNavMenu = true;

    private string? NavMenuCssClass => collapseNavMenu ? "collapse" : null;

    private void ToggleNavMenu()
    {
        collapseNavMenu = !collapseNavMenu;
    }
}
